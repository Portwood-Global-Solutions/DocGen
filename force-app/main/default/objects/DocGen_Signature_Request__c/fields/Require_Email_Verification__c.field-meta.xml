<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Require_Email_Verification__c</fullName>
    <defaultValue>true</defaultValue>
    <description
    >Resolved at send time from the per-send override, template default, then org default (#verification). When true, signers on this request must verify a one-time email PIN before signing; when false, the verification step is skipped. The signing page and the saveSignature/signPlacement gates read this field.</description>
    <externalId>false</externalId>
    <label>Require Email Verification</label>
    <type>Checkbox</type>
</CustomField>
