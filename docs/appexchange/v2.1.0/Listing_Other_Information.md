# AppExchange Listing — "Other Information"

**Field on the Partner Console submission form:** _Other Information — Provide any other information that will help us test your solution._

**Submitted for:** Portwood DocGen Managed v2.1.0 (`04tVx000000Zw5xIAC`), 2026-05-22 re-submission

---

## Paste-ready text

The test org has v2.1.0 installed, the running user is already assigned the `DocGen_Admin` permission set, and sample data is seeded — `Acme Demo Corp` (Account) with 3 contacts plus 3 DocGen templates (`Account Summary`, `Contact Welcome Pack`, `Signature Request — Sales Contract`). The full v2.1.0 source tree is at https://github.com/Portwood-Global-Solutions/DocGen (release tag `v2.1.0`). The seeded signer records have `PIN_Hash__c` pre-populated with `SHA256('000000')` so PIN `000000` works on every sample signer — the test-org email addresses are `@example.com` placeholders and won't actually deliver. Contact dave@portwood.dev with questions during review (24-hour response SLA).

---

_File maintained at `docs/appexchange/v2.1.0/Listing_Other_Information.md` so future re-submissions can pull this verbatim or update in place._
